[build]
  base = "."
  command = "npm install && npm run build"
  publish = "docs/dist"

[build.environment]
  NODE_VERSION = "18"

# Redirect root to docs site (optional - remove if you want different root behavior)
# [[redirects]]
#   from = "/"
#   to = "/docs"
#   status = 301
