---
title: Attributes
description: Configure color-input with HTML attributes
---

import ColorInputDemo from '../../../components/ColorInputDemo.astro';
import { Tabs, TabItem } from '@astrojs/starlight/components';

## Overview

The `<color-input>` element accepts three HTML attributes to configure its behavior:

- **`value`** - The initial/current color value (CSS color string)
- **`colorspace`** - The color space for editing
- **`theme`** - Visual theme (auto, light, or dark)

## value

The `value` attribute sets the color using standard CSS color syntax. It can be any valid CSS color string in the specified color space.

```html
<color-input value="oklch(70% 0.2 240)" colorspace="oklch"></color-input>
```

### Examples by Color Space

<Tabs>
  <TabItem label="OKLCH">
    <ColorInputDemo value="oklch(70% 0.2 240)" colorspace="oklch" />
    ```html
    <color-input value="oklch(70% 0.2 240)" colorspace="oklch"></color-input>
    ```
  </TabItem>
  <TabItem label="HSL">
    <ColorInputDemo value="hsl(240 80% 60%)" colorspace="hsl" />
    ```html
    <color-input value="hsl(240 80% 60%)" colorspace="hsl"></color-input>
    ```
  </TabItem>
  <TabItem label="sRGB">
    <ColorInputDemo value="rgb(20% 40% 80%)" colorspace="srgb" />
    ```html
    <color-input value="rgb(20% 40% 80%)" colorspace="srgb"></color-input>
    ```
  </TabItem>
  <TabItem label="Display P3">
    <ColorInputDemo value="color(display-p3 0.3 0.7 0.95)" colorspace="display-p3" />
    ```html
    <color-input value="color(display-p3 0.3 0.7 0.95)" colorspace="display-p3"></color-input>
    ```
  </TabItem>
</Tabs>

### Common Color Formats

The component accepts many CSS color formats:

<div class="demo-grid">
  <div>
    <strong>Hex Colors</strong>
    <ColorInputDemo value="#3b82f6" colorspace="srgb" showCode={false} />
    <ColorInputDemo value="#10b981" colorspace="srgb" showCode={false} />
    <ColorInputDemo value="#f59e0b" colorspace="srgb" showCode={false} />
  </div>
  <div>
    <strong>RGB Colors</strong>
    <ColorInputDemo value="rgb(239, 68, 68)" colorspace="srgb" showCode={false} />
    <ColorInputDemo value="rgb(168, 85, 247)" colorspace="srgb" showCode={false} />
    <ColorInputDemo value="rgba(236, 72, 153, 0.8)" colorspace="srgb" showCode={false} />
  </div>
</div>

```html
<!-- Hex -->
<color-input value="#3b82f6" colorspace="srgb"></color-input>
<color-input value="#10b981" colorspace="srgb"></color-input>

<!-- RGB -->
<color-input value="rgb(239, 68, 68)" colorspace="srgb"></color-input>
<color-input value="rgba(236, 72, 153, 0.8)" colorspace="srgb"></color-input>
```

:::tip
If `value` is omitted, it defaults to `oklch(75% 75% 180)` (a vibrant cyan).
:::

## colorspace

The `colorspace` attribute specifies which color space to use for editing. Supported values:

- **`oklch`** (default) - Perceptually uniform lightness/chroma/hue
- **`oklab`** - Perceptually uniform lightness/a/b
- **`hsl`** - Hue/saturation/lightness
- **`hwb`** - Hue/whiteness/blackness
- **`srgb`** - Red/green/blue (sRGB)
- **`lab`** - CIE LAB lightness/a/b
- **`lch`** - CIE LCH lightness/chroma/hue
- **`display-p3`** - Wide-gamut Display P3 RGB
- **`rec2020`** - Wide-gamut Rec.2020 RGB
- **`a98-rgb`**, **`prophoto`**, **`xyz`**, **`xyz-d50`**, **`xyz-d65`**

```html
<color-input value="hsl(200 90% 50%)" colorspace="hsl"></color-input>
```

:::note[Color Space Conversion]
The component automatically converts between color spaces. If you set `value` in one space and `colorspace` in another, the value is converted on load.
:::

## theme

The `theme` attribute controls the visual appearance. Supported values:

- **`auto`** (default) - Respects user's system preference
- **`light`** - Always uses light theme
- **`dark`** - Always uses dark theme

<Tabs>
  <TabItem label="Auto">
    <ColorInputDemo value="oklch(65% 0.2 280)" colorspace="oklch" theme="auto" />
    ```html
    <color-input theme="auto"></color-input>
    ```
  </TabItem>
  <TabItem label="Light">
    <ColorInputDemo value="oklch(65% 0.2 280)" colorspace="oklch" theme="light" />
    ```html
    <color-input theme="light"></color-input>
    ```
  </TabItem>
  <TabItem label="Dark">
    <ColorInputDemo value="oklch(65% 0.2 280)" colorspace="oklch" theme="dark" />
    ```html
    <color-input theme="dark"></color-input>
    ```
  </TabItem>
</Tabs>

## Attribute Reflection

All attributes are reflected to properties. Changes to attributes update the corresponding properties, and vice versa:

```js
const picker = document.querySelector('color-input');

// Read attribute
console.log(picker.getAttribute('value'));

// Set attribute
picker.setAttribute('colorspace', 'hsl');

// Both reflect
console.log(picker.colorspace); // "hsl"
```

## Next Steps

- [Properties](/api/properties) - Read and write properties dynamically
- [Methods](/api/methods) - Control the picker with JavaScript methods
- [Events](/api/events) - Listen for color changes and popover events
